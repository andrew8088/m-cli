#  m-cli (PERSONAL FORK)

My personal changes are on the `plugins` branch, so that branch is set as the default branch in my GitHub fork.

I'll use `master` to keep in sync with https://github.com/rgcr/m-cli.
